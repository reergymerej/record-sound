{
  "name": "hello-mic",
  "version": "1.0.0",
  "description": "",
  "main": "hello-mic.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "start": "npm run clear && npm run record",
    "record": "node ./out/hello-mic.js",
    "clear": "rm -f *.wav *.raw",
    "convert": "./bin/convert output.raw",
    "format": "prettier --write src/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mic": "^2.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "prettier": "^3.5.3"
  }
}
